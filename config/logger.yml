level: "debug"
encoding: "json"
outputPaths: [ "stdout", "logs/server.log" ]
errorOutputPaths: [ "stderr" ]
encoderConfig:
    messageKey: "message"
    levelKey: "level"
    levelEncoder: "lowercase"
